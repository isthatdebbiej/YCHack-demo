# Three-minute run

1. Open the GitHub Issue and show the approved `429; never queue` constraint.
2. Show the unchanged demo PR that enqueues overloaded orders.
3. Run Intent Anchor against the issue and PR.
4. Guess aligned; reveal `BADLY DRIFTED` in the TUI.
5. Show the local block, then explicitly trigger the Greptile treatment review.
6. Compare the baseline question with the intent-backed decisive finding.
7. Make a later change under `src/checkout/**` and show Claude-Mem recall with
   zero repeated questions.
